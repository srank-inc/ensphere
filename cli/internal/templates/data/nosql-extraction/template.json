{
  "name": "nosql-extraction",
  "description": "NoSQL operator injection — send $ne/$gt operators to detect query manipulation and data extraction",
  "vuln_type": "nosql",
  "technique": "operator_injection",
  "risk": 3,
  "run_command": "python3 probe.py",
  "observation_fields": ["baseline", "operator_probes", "array_injection"],
  "params": [
    {"name": "TARGET_URL", "description": "Full URL of the API endpoint to test", "example": "http://localhost:3000/api/login", "required": true},
    {"name": "PARAM", "description": "JSON body parameter name to inject operators into", "example": "password", "required": true},
    {"name": "AUTH_HEADER", "description": "Authorization header value", "example": "Bearer eyJ...", "required": false}
  ],
  "files": ["probe.py", "README.md"]
}

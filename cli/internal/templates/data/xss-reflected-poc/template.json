{
  "name": "xss-reflected-poc",
  "description": "Reflected XSS detection — inject payload into parameter and check if it appears unescaped in response body",
  "vuln_type": "xss",
  "technique": "reflected",
  "risk": 2,
  "run_command": "python3 exploit.py",
  "success_signals": ["reflected:true", "context:unescaped"],
  "params": [
    {"name": "TARGET_URL", "description": "Full URL of the page to test", "example": "http://localhost:3000/search", "required": true},
    {"name": "PARAM", "description": "Query parameter name to inject payload into", "example": "q", "required": true},
    {"name": "PAYLOAD", "description": "XSS payload string to inject", "example": "<script>alert(1)</script>", "required": false}
  ],
  "files": ["exploit.py", "README.md"]
}

{
  "name": "ssti-rce",
  "description": "Server-side template injection — inject math expressions across multiple template engines and check for computed results",
  "vuln_type": "ssti",
  "technique": "expression_eval",
  "risk": 3,
  "run_command": "python3 probe.py",
  "observation_fields": ["canary", "baseline", "engine_probes"],
  "params": [
    {"name": "TARGET_URL", "description": "URL of the page/endpoint to test", "example": "http://localhost:3000/render", "required": true},
    {"name": "PARAM", "description": "Parameter name to inject template expressions into", "example": "name", "required": true},
    {"name": "METHOD", "description": "HTTP method (GET or POST)", "example": "GET", "required": false}
  ],
  "files": ["probe.py", "README.md"]
}

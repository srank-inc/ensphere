{
  "name": "cmdi-reverse-check",
  "description": "Command injection via sleep-based timing — inject OS sleep commands and measure response delay for Linux and Windows targets",
  "vuln_type": "cmdi",
  "technique": "command_injection",
  "risk": 3,
  "run_command": "python3 exploit.py",
  "success_signals": ["timing_delta:>4s", "payload_timing_vs_baseline"],
  "params": [
    {"name": "TARGET_URL", "description": "URL of the endpoint to test", "example": "http://localhost:3000/api/ping", "required": true},
    {"name": "PARAM", "description": "Parameter name to inject into", "example": "host", "required": true},
    {"name": "METHOD", "description": "HTTP method (GET or POST)", "example": "POST", "required": false}
  ],
  "files": ["exploit.py", "README.md"]
}
